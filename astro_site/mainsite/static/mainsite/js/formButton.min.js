let formButton=document.getElementById("sendForm");const updateButton=()=>{formButton.setAttribute("aria-busy",true);setTimeout(()=>{formButton.setAttribute("aria-busy",false)},5000)};formButton.addEventListener("click",updateButton);